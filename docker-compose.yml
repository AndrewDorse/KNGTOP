# KNGTOP - copy .env.example to .env, set POLY_DRY_RUN=false for live spike-pair trading
services:
  kngtop:
    build: .
    pull_policy: never
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
